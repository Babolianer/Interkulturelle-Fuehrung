{
  "name": "interkulturelle-fuehrung",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "prisma generate --schema=backend/prisma/schema.prisma && cd frontend && npm ci && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-validator": "^7.2.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}

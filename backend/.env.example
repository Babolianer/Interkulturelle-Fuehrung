# Supabase: Connection string aus Dashboard → Project Settings → Database
# Unter "Connection string" URI wählen und [YOUR-PASSWORD] durch das Datenbank-Passwort ersetzen.
# Direktverbindung (z. B. für Migrations): Port 5432
# Pooler (empfohlen für App): Port 6543, am Ende ?pgbouncer=true
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# JWT (in Produktion starken geheimen Schlüssel setzen)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES=24h

# Server-Port
PORT=3000

# Admin-User für Seed (optional; Standard: admin@interkulturelle-fuehrung.de / Admin123!)
ADMIN_EMAIL=admin@interkulturelle-fuehrung.de
ADMIN_PASSWORD=Admin123!
